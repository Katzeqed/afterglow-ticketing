# Значения по умолчанию совпадают с docker-compose.yml.
# Локальный запуск без Docker — раскомментируй и поправь при необходимости.

# DATABASE_URL=postgresql+psycopg://afterglow:afterglow@localhost:5432/afterglow
# RABBITMQ_URL=amqp://guest:guest@localhost:5672//
# REDIS_URL=redis://localhost:6379/0
